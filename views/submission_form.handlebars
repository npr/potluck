<form class="form-horizontal">

    <!-- General submission information section -->
    <div class="control-group">
        <label class="control-label" for="inputName">Your Name</label>
        <div class="controls">
            <input type="text" id="inputName" placeholder="Your Full Name">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="inputEmail">Your Email</label>
        <div class="controls">
            <input type="text" id="inputEmail" placeholder="Email">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="inputTheme">Call-Out:</label>
        <div class="controls">
            <select id="inputTheme" class="select2">
                {{#callouts}}
                <option value="{{code}}">{{title}}</option>
                {{/callouts}}
            </select>
        </div>
    </div>
    <!-- End of general submission information section -->

    <div class="control-group">
        <div class="controls">
            <label class="checkbox">
                <input type="checkbox"> I hereby confirm that I am the copyright holder on the submitted content or have
                all legal rights to submit the content.
            </label>
            <button type="submit" class="btn">Submit</button>
        </div>
    </div>



</form>

<!--

1. Contact Name – required. Text field
2. Contact Email – optional, Text field
3. Theme/Call-out - auto–populated (depending how you got there) with an option to change to any active call-out (up to latest 10)

-->