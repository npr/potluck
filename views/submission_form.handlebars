<form class="form-horizontal" action="/submission/save" method="post">

    <!-- General submission information section -->
    <div class="control-group">
        <label class="control-label" for="inputName">Your Name</label>
        <div class="controls">
            <input type="text" id="inputName" placeholder="Your Full Name">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="inputEmail">Your Email</label>
        <div class="controls">
            <input type="text" id="inputEmail" placeholder="Email">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="inputTheme">Call-Out</label>
        <div class="controls">
            <select id="inputTheme" class="select2">
                {{#callouts}}
                <option value="{{code}}">{{title}}</option>
                {{/callouts}}
            </select>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label" for="inputMessage">Message</label>
        <div class="controls">
            <textarea rows="5" id="inputMessage" placeholder="Short Message"></textarea>
        </div>
    </div>
    <!-- End of general submission information section -->

    <!-- File uploads section -->
    {{#resources}}
        <fieldset>
            <legend>File Upload #{{num}}:</legend>

            <div class="control-group">
                <label class="control-label" for="inputFile{{num}}">File</label>
                <div class="controls">
                    <input type="file" id="inputFile{{num}}">
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputCaption{{num}}">Caption</label>
                <div class="controls">
                    <input type="text" id="inputCaption{{num}}" placeholder="Caption describing the upload">
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputCopyright{{num}}">Copyright</label>
                <div class="controls">
                    <input type="text" id="inputCopyright{{num}}" placeholder="Copyright Info">
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputLocation{{num}}">Location</label>
                <div class="controls">
                    <input type="text" id="inputLocation{{num}}" placeholder="Location Info">
                </div>
            </div>

            <div class="control-group">
                <label class="control-label" for="inputDate{{num}}">Date</label>
                <div class="controls">
                    <input type="text" id="inputDate{{num}}">
                </div>
            </div>

        </fieldset>
    {{/resources}}
    <!-- End of file uploads section -->

    <div class="control-group">
        <div class="controls">
            <label class="checkbox">
                <input type="checkbox"> I hereby confirm that I am the copyright holder on the submitted content or have
                all legal rights to submit the content.
            </label>
            <button type="submit" class="btn">Submit</button>
        </div>
    </div>



</form>

<!--

1. Contact Name – required. Text field
2. Contact Email – optional, Text field
3. Theme/Call-out - auto–populated (depending how you got there) with an option to change to any active call-out (up to latest 10)


5.1. Caption – optional, free-form text field
5.2. Copyright – optional, free-form text field
5.3. Location – optional, free-form text field
5.4. Date – optional, a date-picker widget
5.5. Consent checkbox which users need to check to indicate that they do indeed have the right to distribute the content they are submitting.
-->